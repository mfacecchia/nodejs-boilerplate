<head>
    <head>
        <meta chartset = "UTF-8">
        <meta name = "viewport" content = "width=device-width, initial-scale=1.0">
    
        <link rel = "stylesheet" href = "/style/style.css">
        <link rel = "icon" href = "/assets/icons/favicon.webp">
        <!-- Google fonts import -->
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet">
    
        <%# Include custom stylesheets when including this partial in your page (optional) %>
        <% if(typeof stylesheets !== 'undefined'){ stylesheets.forEach(stylesheet => { %> <link rel = "stylesheet" href = <%= stylesheet %>> <% })} %>
    
        <title>
            Feis' NodeJS Boilerplate
            <%# Set custom title for each page (optional) %>
            <% if(typeof title !== 'undefined'){ %> - <%= title %> <% } %>
        </title>
    </head>
</head>